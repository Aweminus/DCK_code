"""DCK: Descendant Closure Knockout for protected compaction in long-horizon search agents.

This package is additive: the baseline loop in react_agent.py is kept
byte-compatible (only its host-trigger expression is routed through dck.host so
that every experimental arm shares identical trigger code, Spec 5.8).
"""

__version__ = "0.1.0"
